@model Quiron.LojaVirtual.Dominio.Entidade.Carrinho

<div class="navbar-right">
   @Html.ActionLink("Checkout", "Index", "Carrinho", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "btn btn-default navbar navbar-btn" })
</div>

<div class="navbar-text navbar-right">
   <b>Seus produtos</b>
   @Model.ItensCarrinho.Sum(x => x.Quantidade) item(s), @Model.ObterValorTotal().ToString("c")
</div>